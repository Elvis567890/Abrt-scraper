name: Build and Push to ghcr.io

on:
  schedule:
    - cron: '0 8 * * *'          # Runs every day at 08:00 UTC
  push:
    branches: [ main ]
  workflow_dispatch:             # Allow manual trigger

jobs:
  build-and-scan:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/abrt-scraper:latest

      - name: Run scraper
        run: |
          docker run --rm \
            -e SECRET_KEY="${{ secrets.SECRET_KEY }}" \
            -e DATABASE_URL="${{ secrets.DATABASE_URL }}" \
            -e ADMIN_EMAILS="${{ secrets.ADMIN_EMAILS }}" \
            -e ENABLE_SCHEDULER="false" \
            ghcr.io/${{ github.repository_owner }}/abrt-scraper:latest \
            python -c "from scraper import run_scan_and_store; run_scan_and_store()"

      # Optional: Upload the generated files as artifacts for inspection
      - name: Upload opportunities file
        if: always()   # even if scan fails, we can still see partial results
        uses: actions/upload-artifact@v3
        with:
          name: opportunities
          path: current_opportunities.json
